---
name: auto-paper
description: 任意ドメインの表形式データ（CSV/TSV/Excel 由来の患者レベル・顧客レベル・店舗レベル・ログ等）から、研究課題をもとに監査ゲート付きで投稿可能水準の論文/レポートドラフトを生成する。7工程・4人間ゲート(G0/G1/G1.5/G2)・3層監査(決定的アサーション→LLM独立監査→cross-model外部議論)で、非回答/センチネルコードの実数値混入・Methods↔コード乖離・幻覚引用・N不整合・値域外れ値・単位混在といった機械的欠陥を構造的に検出する。臨床・疫学に限らず業務・経済・教育・運用データに適用できる。任意のCSVやテーブルデータの解析・論文化・レポート作成・データ品質監査・変数コーディング検証・投稿前チェックに関わる依頼で使うこと。
---

# auto-paper 統括オーケストレータ

任意ドメインの表形式データを対象とした論文/レポート自動生成パイプラインの**統括**本体。統括は工程を横断して読み、工程遷移を確認する唯一の主体であり、実際の設計・分析・執筆・監査は各工程の独立サブエージェントに委譲する。

## 0. 監査コアは同梱してあるが、原本は別にある（重要）

`audit/`（19の決定的チェック）と `pipeline/`（スキーマ・プロファイラ・ゲート）は**別スキルが保持する実装が原本**であり、本スキルは `core.py` 経由でそれを解決する。

ただし clone しただけで動くよう、原本の写しを `vendor/core/` に**同梱**してある（`sync_core.py` が生成、stdlib のみ・51ファイル）。同梱は二重管理を生みやすいので:

- **解決順序で原本（隣接スキル）が同梱物より優先される。** コアを直せば即反映される
- `vendor/PROVENANCE.json` が原本のコミットと全ファイルの sha256 を記録し、`sync_core.py --check` と `tests/test_vendor.py` が改竄・ドリフトを検出する
- **`vendor/core/` を直接編集しないこと。** 原本を編集して `sync_core.py --source <原本>` で再同期する
- 本スキル固有の実装は4つだけ: `SKILL.md` / `config.yaml` / `agents/` / `references/domains/*.json`。ドメインを増やすなら `references/domains/<name>.json` を足すだけでよい

コアの特定は**ディレクトリ名ではなく構造**で行う（`audit/tier1/runner.py` 等を備えたディレクトリを skills 配下から探す）。名前に依存しないのでコア側のリネーム・移動で壊れない。候補が複数見つかった場合は曖昧なので自動選択せず失敗する（`CoreAmbiguous`）。コアが見つからない場合も**明示的に失敗する**（`CoreNotFound`）。監査を実行せずに正常終了する経路は作っていない。

### 変数の一次ソースについての正直な開示

本スキルの check A（変数コーディング照合）は、**自動データプロファイル（ヒューリスティック）＋任意ユーザー辞書**を一次ソースとする。設問票の選択肢ラベル原文のような**確定した ground truth に照合しているわけではない**。したがって偽陰性（センチネルの取りこぼし）のリスクは本質的に残り、最終防波堤は G1 の人間確認である。この非対称を「機械保証済み」と読み替えないこと。

設問票が機械可読で手に入る調査データを扱う場合は、設問票を一次ソースにする専用プロファイルの方が検出力が高い。

## 1. ドメインの扱い（チェック集合はドメインで変わらない）

`domain` は `clinical` / `survey` / `general` のいずれかを run ごとに宣言する。**既定値は無い**（暗黙に `general` へ倒すと「値域を照合しなかった」ことに気づけないため）。

素朴には「臨床なら値域チェック有効、汎用なら無効」にしたくなるが、それは誤りである。値域外れ値（D）・単位混在（U）・打ち切り（L）・不死時間バイアス（I）はいずれも臨床固有の概念ではない:

- 「ありえない値」は業務データにも普通にある（負の売上、在庫 -1、年齢 999、負の所要時間）
- 単位混在はむしろ経済データの方が深刻（円/千円/百万円、名目/実質）
- 打ち切りは所得のトップコーディングで日常的に起きる
- 不死時間バイアスは「曝露が確定する前の期間を追跡に含める」設計上の誤りで、縦断データならドメインを問わず成立する

臨床固有なのはチェックの**ロジック**ではなく**参照データ**（生理的値域・単位換算表）だけである。したがって:

- **チェック集合は全ドメインで同一**（コアの `run_tier1_generic` をそのまま使う）
- **ドメインは参照データのパックを選ぶだけ**

`general` パックは空だが、それは「チェックしない」意味ではない。値域宣言が無い変数は check D が照合対象を持たないため、その事実を工程5が「値域未宣言」として警告する。SAP（工程1）で `impossible_ranges` / `plausible_ranges` を宣言すれば、臨床とまったく同じ強度で決定的に照合される。

**優先順位: 研究ごとの SAP 宣言 ＞ ドメインパック既定値。**

## 2. 到達目標（正直な再設定）

「研究課題 → 投稿可能論文の完全自動生成」は**完全には達成不可能**である。識別戦略の新規性・妥当性判断、標本の限界に関する学術的スタンス、データ利用規約・共著は制度問題であり LLM の管轄外にある。

約束する到達目標は3点のみ:

1. **機械的欠陥クラスをゼロにする**（センチネル混入・転記ミス・記載↔実装乖離・幻覚引用・N不整合）
2. **機械保証付きドラフトを生成し、人間監査コストを圧縮する**
3. **学術判断が要る箇所は人間ゲート（G0/G1/G1.5/G2）を最終防波堤に置く**

### 保証範囲外（コアから継承する正直な開示）

- **英語原稿を生成しない。** テンプレートと執筆エージェントは日本語で、翻訳・英文校正工程は存在しない。check S は語彙辞書マッチであり英文校正の代わりにならない。
- **数値の再現アサーションは未実装。** プレースホルダ機構が消すのは**転記エラー**だけで、「`results.json` が生データから再現するか」は保証していない。保証しているのは「原稿の数値が `results.json` と一致すること」まで。
- **precision は実証されていない。** 新規チェックを足すほど偽陽性が積み上がる構造であり、「達成済み」と述べてはならない。
- **本スキルの check A は偽陰性リスクが高い。** 一次ソースがヒューリスティックなプロファイラであるため、取りこぼしの最終防波堤は G1 の人間確認である。

## 3. 工程とゲート

| 工程 | 内容 | 成果物 | ゲート |
|---|---|---|---|
| 1 | 設計（RQ→PECO・デザイン・SAP） | `00_spec/design_protocol.md`, `sap.md`, `sap_ranges.json` | **G0**（人間） |
| 2 | 変数（プロファイル・コーディング・センチネル隔離） | `01_data/data_profile.json`, `variable_codebook.json`, `user_dictionary.json` | **G1**（人間） |
| 3 | 分析（SAP 実行） | `03_results/results.json`, `flow.json`, `methods_claims.json`, `code_filters.json` | — |
| 4 | 執筆（プレースホルダ原稿） | `04_manuscript/manuscript.md` | — |
| 5 | 監査（Tier1/2/3） | 監査レポート | **G1.5**（閾値超過時） |
| 6 | 自己修復（stage-aware 巻き戻し） | — | コーディング変更は G1 再通過 |
| 7 | 最終判定 | — | **G2**（人間） |

工程1〜4のプロンプトは `agents/01..04_*.md`（本スキル）。監査系プロンプト（`05_audit.md` / `negation.md` / `adjudicator.md`）と 3層監査の機構はコア側を共有する。

**既知の重複**: コア側にも同目的の工程プロンプト一式（`agents/generic/01..04_*.md`）が存在する。監査コードの非複製は守っているが、エージェント定義はこの1点だけ2系統が併存しており、放置するとドリフトする。汎用 run の正は本スキルの `agents/` 側とする。非干渉モデル・入力契約・自己修復の詳細はコアの `SKILL.md` §1〜§7 および `references/DESIGN.md` を参照。

## 4. 使い方

```bash
# 工程5（監査）の実走
python3 run_audit.py --run-dir <成果物ディレクトリ> --domain general
python3 run_audit.py --run-dir <run> --domain clinical --data-csv data.csv --json findings.json
```

終了コード: `0`=指摘なし / `1`=FAIL または INCOMPLETE あり（人間確認要） / `2`=**監査を実行できなかった**（コア未解決・入力欠落・ドメイン未宣言）。

`2` を `1` と区別するのは重要である。監査が動かなかったことを「不合格」と同じ扱いにすると、設定ミスを検出結果と読み違える。

必要な入力（`--run-dir` 配下、コア側スキーマと同一）:

```
01_data/variable_codebook.json      必須                       -> check A
01_data/user_dictionary.json        任意（書くほど監査が強い）   -> check A の格上げ
01_data/data_profile.json           任意（無ければ CSV から生成）-> check A/D/L
03_results/methods_claims.json      必須                       -> check B
03_results/code_filters.json        必須                       -> check B
03_results/flow.json                任意                       -> check K
03_results/bibliography.json        任意                       -> check M（引用実在）
03_results/sap_ranges.json          任意                       -> check D/E/G/I/J/K/L/O/S/T/U/W ほか
```

### 供給しなかったチェックは「未実行」として必ず表面化する

コアは optional 入力が未供給のチェックを**意図的スキップ**として扱い、finding を1件も出さない。素朴に呼ぶと「19チェック中2つしか走っていないのに `PASS=2 FAIL=0 INCOMPLETE=0` で exit 0」になる（2026-07-30 の外部レビューで実際にこの欠陥が見つかり、修正した）。

そこで本ドライバは `config.yaml` の `profiles.default.checks` を**実際に読み**、finding も coverage proof も出なかったチェックを「入力未供給で未実行」として INCOMPLETE 報告する。したがって:

- 入力を揃えない限り **exit 0 にはならない**
- 出力末尾の `coverage:` に、走ったチェックの照合件数と**未実行チェックの一覧**が出る
- `--json` の `unrun_checks` / `coverage` にも同じ情報が入る

各チェックに何を供給すれば走るかは、未実行として報告される際のメッセージに明示される（対応表は `run_audit.py` の `CHECK_REQUIREMENTS`）。

### check D の観測値はデータ由来（自己申告ではない）

値域照合に使う観測値は `data_profile` の列ごとの min/max から機械的に導出する。解析エージェントが `sap_ranges.json` に書いた自己申告値は使わない。宣言値域と宣言観測値を突き合わせると両辺が同じ成果物由来になり、本スキルが消すと約束した転記エラーのクラスに無力になるためである。

### ユーザー辞書を書くと監査が強くなる

`user_dictionary.json` で `missing_codes` を宣言した列では、センチネル誤用が **INCOMPLETE ではなく確定的 FAIL** になる（根拠が authoritative になるため）。

```json
{ "satisfaction": { "missing_codes": [99], "allowed_values": [1,2,3,4,5] } }
```

辞書が無い場合、プロファイラのヒューリスティック候補のみが根拠となり、誤用は INCOMPLETE として人間レビューに回る（サイレント PASS にはならないが、確定的ブロックにもならない）。

## 5. 本スキルの起源となった失敗（ドメインを問わず再発する）

元の事例では、収入設問の選択肢19「答えたくない」・20「わからない」に 2,500万円という実数値が割り当てられ、解析標本の21.7%が非回答のまま収入値を持っていた。同型の失敗は任意のドメインで起きる:

- 調査: 満足度 99＝無回答を「99点」として平均に算入
- 業務: 欠品を -1 で表現し、在庫平均が負に引きずられる
- ログ: タイムアウトを 9999ms で記録し、応答時間分布が壊れる
- 人事: 「非該当」を 0 として勤続年数に算入

連続量の passthrough 変数（`numeric_map` 無し）ほど宣言漏れが致命的である。工程2の `treat_as_missing` 宣言と G1 がこれを止める。

## 6. テスト

```bash
python3 -m pytest        # 本スキル（コア解決・コア契約・ドメインパック・ドライバ統合・非臨床データでの実走）
```

非臨床フィクスチャ `tests/fixtures/retail.csv` に、満足度99（非回答コード）と売上 -1（ありえない値）を仕込んであり、臨床用語を一切含まないデータで両方が捕まることを固定している。コア側のテストはコアを保持するスキル側で実行する。
